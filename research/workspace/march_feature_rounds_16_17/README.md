# March Mania — rounds 16 and 17

Prepared, static-reviewed experimental code; no executed results are bundled for these new rounds.

Start with **START_HERE.md**. User runs `run_tests.py`, then notebook 16's smoke/replay gate and remaining cells, followed independently by notebook 17. Both protocols define four adjusted effects, two families, four unadjusted controls, fixed classifiers, family ablations, and ten inline Plotly charts.

`VALIDATION.json` separates static inspection from pending runtime validation. `MILESTONE_14_15_REVIEW.md` summarizes the actual supplied results. `GIT_WORKFLOW.md` is an optional manual publication procedure after execution and review; nothing is committed automatically.
