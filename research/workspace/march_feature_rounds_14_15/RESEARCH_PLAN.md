# Two-round research plan

Read the equally scoped specifications:

- **Round 14:** [Ranking resolution and cardinal mappings](ROUND_14_PROTOCOL.md).
- **Round 15:** [Common-opponent, same-venue comparisons](ROUND_15_PROTOCOL.md).

Both contain four candidates in two families, five configurations, sixteen maximum
new classifier fits, four replayed consensus references, twelve feature snapshots,
zero rating fits, controlled family ablations, one exact-duplicate sensitivity control,
strict upstream preservation, individual fit receipts, ten Plotly figures, and a return ZIP.

They run sequentially for compute safety, not because round 15 depends on round 14's
scientific result. Do not alter either specification after observing the other result.
Run both and return `reports/rounds_14_15_return.zip`.

The validated ranking-consensus signal still needs a separate production-recipe transfer
check. These new experiments neither replace that obligation nor claim to have beaten
the historical leaderboard. The current score gap is not proven to be exclusively a
feature-engineering gap. The fixed learner isolates representation tests.
