# Public proof; private implementation

The connected GitHub read confirmed the original repository exists publicly and still has main `84b8fb36644a6558beded6dad84f5645ea4405d3`. User-level push permissions are not the same as a commit/push tool in this chat. The exposed connector actions were inspected; no write action exists here. No remote commit/push/merge/visibility change was attempted or claimed.

Your current explicit request to help with committing/pushing was treated as a narrow publication request, not authorization for scientific execution or AWS changes. The new helpers are commands **you run**, not background agents.

## The two destinations

- Private `march-mania-research`: explicit current source/test/configuration snapshots under `snapshots/<id>`, with excluded/missing paths and file hashes. Not a dataset/model backup and not a migration of complete old Git history.
- Public `march-mania-portfolio`: presentation and aggregate evidence only. It includes a report-only notebook; it does not publish training source, exact private formulas, weights, or individual predictions.

Both are created only by your `publish` invocation after review. Existing destinations must have the correct owner and visibility; otherwise the command stops. It never converts a public repo to private or the reverse without you.

## Your original public research repository

Creating a private snapshot does not make the original public code disappear. After verifying the two new destinations, open the original repository's Settings → General → Danger Zone → Change repository visibility, and choose Private if you decide to stop exposing that implementation. Confirm on GitHub's own page. This changes remote access, not the local working files; future private fetches require authorized access. The helpers do not perform this administrative change.

Existing public forks and local copies may remain. Earlier distributed MIT-licensed material remains governed by that license; no retroactive revocation is claimed. No notice makes public files technically uncopyable. Get qualified legal advice before changing license terms for contributed/third-party code.

No secret removal or history purge is attempted here. Revoke/rotate actually exposed credentials first if an exposure is discovered; changing visibility is not credential remediation.

## What "synchronized" means here

After the merge command, the *separate publication clones* match their fetched default branches and listed published file hashes. The original scientific checkout stays pinned, keeping experiment provenance valid. Sources changed since a plan need a new reviewed snapshot. Raw data, live notebook outputs, model artifacts, unpublished files and complete old Git history are outside this snapshot certificate.

## Official documentation
- https://cli.github.com/manual/gh_auth_login
- https://docs.github.com/en/apps/oauth-apps/building-oauth-apps/authorizing-oauth-apps
- https://cli.github.com/manual/gh_pr_merge
- https://docs.github.com/en/pull-requests/reference/forks
- https://docs.github.com/en/enterprise-cloud@latest/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/licensing-a-repository
