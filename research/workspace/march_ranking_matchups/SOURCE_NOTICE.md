# Source and evidence notice

The unchanged files `frozen/research_workflow.py`, `frozen/shot_features.py`, and `research_io.py` come from the earlier user-owned manual research kits. Their original hash identities are recorded in `constraints.json` and `MANIFEST.json`. They implement the frozen reference and verified JSON replay. No external models or copyrighted datasets are bundled.

`evidence/round11/` preserves a subset of the user's returned aggregate evidence. `return_integrity.json` is the original complete archive manifest; some listed members are intentionally not copied into this subset. The complete local prior archive is separately verified before running.

No raw competition CSVs, weights, private AWS files, credentials, fonts, or personal email are included. Online sources are attributed in RESEARCH_PLAN.md; their formulas are not claimed to validate the custom new features.
